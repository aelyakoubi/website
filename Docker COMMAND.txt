
docker-compose down               OFF / Remove

docker-compose up --build         ON  / Build 




Rebuild the Docker image to ensure a fresh installation of all dependencies:

docker-compose build --no-cache

Start the containers again:

docker-compose up
